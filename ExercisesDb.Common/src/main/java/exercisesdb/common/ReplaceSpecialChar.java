package exercisesdb.common;

public class ReplaceSpecialChar {
    public static String replaceChar(String chars, String oldChar, String newChar) {
        return chars.replace(oldChar, newChar);
    }
}
